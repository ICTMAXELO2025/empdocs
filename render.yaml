services:
  - type: web
    name: employee-doc-manager
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: wil_database
          property: connectionString
      - key: SECRET_KEY
        generateValue: true